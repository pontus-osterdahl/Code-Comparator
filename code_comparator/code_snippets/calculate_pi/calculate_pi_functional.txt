int threadCount = 4;
int n = 100000;

double pi = Stream.iterate(0, u -> u + 1)
  .limit(threadCount)
  .parallel()
  .map(e -> calcPi(threadCount,e,n))
  .reduce(0.0, (a, b) -> a + b);

System.out.println("\nPI: " + pi * 4);