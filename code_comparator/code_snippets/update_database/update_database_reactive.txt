Database db = Database.test();

System.out.println("PERSONS BEFORE UPDATE");
        
db.select("select name from PERSON")
.getAs(String.class)
.blockingForEach(System.out::println);

//db.update("INSERT INTO person (NAME,SCORE) values(\'Nisse\', 23)") 
  //      .counts()
    //    .blockingForEach(e -> {});


System.out.println("PERSONS AFTER UPDATE");

//db.select("select name from PERSON")
//.getAs(String.class)
//.blockingForEach(System.out::println);
	    
		
