ArrayList<ArrayList<Integer>> arr_a = getArray();
ArrayList<ArrayList<Integer>> arr_b = getArray();

System.out.println("Array a");
printArrayList(arr_a);
System.out.println();
System.out.println("Array b");
printArrayList(arr_b);
System.out.println();

int rows = arr_b.size();

Observable.range(0, rows)
  .concatMap(nr -> Observable.just(nr) 
    .map(rowNr -> multiplyRow(rowNr,arr_a, arr_b))
      .subscribeOn(Schedulers.computation())
    )
  .toList()
  .subscribe(e -> {System.out.println("Result"); printArrayList(e);});

  